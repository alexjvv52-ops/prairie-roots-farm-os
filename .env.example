# Copy to .env and fill in locally. Never commit .env.

# Vite / Tauri dev (optional)
TAURI_DEV_HOST=

# Cloudflare Worker (checkout-endpoint) — set via wrangler secret put in deploy,
# or locally in .dev.vars (never commit .dev.vars)
STRIPE_RESTRICTED_KEY=
ALLOWED_ORIGIN=
SUCCESS_URL=
CANCEL_URL=
